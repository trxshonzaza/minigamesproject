package com.trxsh.minigames.utility;

import org.bukkit.ChatColor;

public enum TeamType {

    RED(ChatColor.RED + "" + ChatColor.BOLD + "Red Team" + ChatColor.WHITE),
    BLUE(ChatColor.BLUE + "" + ChatColor.BOLD + "Blue Team" + ChatColor.WHITE);

    TeamType(String s) {

    }
}
